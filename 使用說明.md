# 批次自動蓋章工具 — 使用說明

一鍵把「主管章」蓋到同仁交來的 PDF / JPG / PNG 檔案上，並自動避開文字與簽名的位置。

## 功能特色

- **批次處理**：一次選一整個資料夾，全部自動蓋完。
- **支援格式**：`.pdf`、`.jpg` / `.jpeg`、`.png`
- **PDF 只蓋最後一頁**：符合一般公文簽核蓋在最後一頁的習慣。
- **智慧空白偵測**：在文件右下角一塊範圍內，自動尋找最空白（沒有文字、線條）
  的位置蓋章，避免蓋到內容上；若找不到理想位置，會自動退回固定的右下角。
- **自動去除章的白色背景**：不管你用的是去背 PNG 還是普通白底 JPG 印章照片，
  程式都會把白色背景去掉，蓋上去乾乾淨淨，不會有白色方塊。
- **不覆蓋原始檔**：輸出的檔案會存到另一個資料夾，檔名自動加上「_已蓋章」。

## 安裝步驟（第一次使用才需要）

1. 安裝 Python 3.9 以上版本（Windows 可到 python.org 下載，安裝時記得勾選
   「Add Python to PATH」）。
2. 開啟終端機 / 命令提示字元，切換到本資料夾，執行：

   ```
   pip install -r requirements.txt
   ```

   > 如果是 Linux 且執行時出現「找不到 tkinter」的訊息，請另外執行：
   > `sudo apt install python3-tk`

## 使用方式

1. 執行程式：

   ```
   python stamp_tool.py
   ```

2. 在視窗中依序設定：
   - **① 待蓋章資料夾**：放同仁交來的 PDF/JPG/PNG 的那個資料夾。
   - **② 主管章圖片**：主管印章的圖片檔（建議準備一張清楚、背景乾淨的照片
     或掃描檔，白底或去背 PNG 都可以）。
   - **③ 輸出資料夾**：處理完的檔案要存去哪裡（請不要跟①同一個資料夾）。
   - **④ 章的大小**：章的寬度佔文件寬度的百分比，預設 14%，可依需求調整
     （例如章太大/太小可以調整這個數字，5~40 之間）。
3. 按下「開始批次蓋章」，下方會即時顯示每個檔案的處理進度與結果。
4. 完成後到輸出資料夾檢查蓋章效果即可。

## 常見問題

**Q: 蓋章位置跑掉了怎麼辦？**
目前是在「右下角一塊範圍」內自動找空白處，如果文件排版特殊（例如整頁都是
表格、右下角剛好有簽名欄），可能會找不到很空白的地方，這時程式會自動退回
固定的右下角位置，不會蓋失敗，但位置不一定完美，建議蓋完後抽查幾份確認。

**Q: 想要蓋在其他位置（例如左下角、正中央）怎麼辦？**
可以請人依需求微調程式中 `get_search_region()` 函式裡的參數，或改成固定座標
版本，這部份可以再客製。

**Q: PDF 想要每一頁都蓋章，不是只蓋最後一頁？**
可以修改 `batch_process` 呼叫 `stamp_pdf_file` 時的邏輯，讓它對每一頁都執行
一次蓋章（目前為了符合「只蓋最後一頁」的需求，寫死只處理 `doc[-1]`）。

**Q: 執行時說找不到 PyMuPDF 或 tkinter？**
代表套件沒裝好，回到「安裝步驟」重新執行 `pip install -r requirements.txt`，
Linux 使用者另外要裝 `python3-tk`。

## 想要打包成不用裝 Python 的執行檔（.exe）？

可以額外安裝 `pyinstaller` 後執行：

```
pip install pyinstaller
pyinstaller --onefile --windowed --name 批次蓋章工具 stamp_tool.py
```

打包完成後，`dist` 資料夾裡會有一個 `批次蓋章工具.exe`，同事的電腦上不需要
安裝 Python 也能直接雙擊使用。
